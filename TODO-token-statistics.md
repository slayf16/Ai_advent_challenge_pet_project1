# DeepChat — актуальный TODO после дополнительной итерации

Обновлено оркестратором по результатам независимого review и реального API-теста. После review исполняемый код не менялся. Коммит/push/deploy не выполнялись.

## Осталось исправить

- [ ] **P2 — связи user/request теряются после reload.** В hooks/use-chat.ts нормализаторы metrics/message не возвращают requestId/requestIds. Обычный запрос и retry до восстановления имеют корректные 1/1 связи, после JSON roundtrip через recoverSession IDs исчезают и входная статистика у user пропадает. Места: hooks/use-chat.ts:183,235; app/page.tsx:447. Сохранить и строго восстановить эти идентификаторы; проверить обычный запрос, retry и повторный reload.
- [ ] **P2 — идентификаторы экспертов меняются после привязки.** initialExperts связаны с user, затем при запуске создаются новые metrics с новыми IDs. В успешной группе из четырёх вызовов совпадает только ID синтеза; три строки входа показывают «Нет данных» при существующем usage. Места: hooks/use-chat.ts:613,643. Сохранять один ID физического запроса от создания до завершения и восстановления.
- [ ] **P2 — общий таймер учитывает время офлайн при неизвестном конце.** Индивидуальный duration теперь null, но aggregate использует now для cancelled endedAt=null: через сутки/двое показывает 86400000/172800000 мс. Смешанная performance/epoch шкала тоже даёт растущее значение. Место: lib/chat-metrics.ts:306. Распространить обработку неизвестного времени на aggregate без выдуманного окончания и смешения шкал.
- [ ] **P2 — cache-only/reasoning-only usage не сохраняется.** Корректный reasoning_tokens=40 при неизвестном completion теряется после restore; аналогично поля кеша без основных totals. Место: hooks/use-chat.ts:136–147. Сохранять корректные независимые поля, не вычисляя неизвестный остаток ответа и не подставляя нули.
- [ ] **Текст:** заголовок общего блока остаётся «За весь диалог», согласован «Расход за весь диалог» (app/page.tsx:404).
- [ ] **Покрытие:** добавить регрессионные тесты для перечисленных выше случаев. Существующие 66 тестов проходят, но не покрывают эти сценарии. Воспроизведения есть в scratch work/api-only-fix2-repro.mjs текущего задания.

## Подтверждено исправленным

- [x] Убраны приблизительные оценки текста и денежные ориентиры UTF8/4.
- [x] Обычный запрос и retry показывают вход API по явным связям до reload.
- [x] Completion-only и prompt-only usage сохраняются после restore; выходная цена считается из completion и снимка тарифа.
- [x] У ответа показываются выходные токены и разложение на ответ/рассуждения при достоверном reasoning.
- [x] Полная цена запроса присутствует в details при достаточных данных.
- [x] Покрытие токенов и тарифов объясняется раздельно.
- [x] Индивидуальная длительность восстановленного запроса при неизвестных/несовместимых временах не выдаётся за известное время.
- [x] Сумма API группы учитывает четыре физических вызова один раз: mock 10/5/15 на вызов → 40/20/60.
- [x] README metrics paragraph обновлён под статистику API.

## Реальный тест

Один POST /api/chat через реальный streamChat проекта. Запрос: «привет, подскажи пожалуйста какие типы ядерных реакторов бывают». Запрошена default deepseek-v4-flash, API вернул deepseek-flash. Успешный stop, 9.782 секунды.

- Вход49 + выход1891 = всего1940.
- Рассуждения860 входят в выход; остаток ответа1031.
- Cache hit0 / miss49.
- Расчётная цена по snapshot Flash: вход $0.00001078, выход $0.00124806, всего $0.00125884.
- UI не проверен: доступных браузеров нет. Live API проверен прямым вызовом localhost; повторного запроса не было.

Пути и строки относятся к проекту C:\Users\AMK29\Documents\Codex\2026-09-03\new-chat-3\outputs\deepseek-chat на момент review. Этот TODO отражает текущий результат и заменяет старый список четырёх пунктов.
